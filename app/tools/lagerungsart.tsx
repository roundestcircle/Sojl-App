import { View } from 'react-native';
import LagerungsartTool from '@/components/LagerungsartTool';

export default function LagerungsartScreen() {
  return (
    <View style={{ flex: 1 }}>
      <LagerungsartTool />
    </View>
  );
}
